### write a python program to reverse a word after accepting the input from the user( No validations are added in the code)
#Inputword = ineuron
#output word = norueni

iput = input("Enter the word  : ")
print(iput)
r_iput  = str(iput[::-1])
print(r_iput)